<UserControl x:Class="Demo.Views.PersonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Demo.Views"
             xmlns:viewmodels="clr-namespace:Demo.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">
    <!--<UserControl.DataContext>
        <viewmodels.PersonViewModel/>
    </UserControl.DataContext>-->
    
    <!--<StackPanel Background="AliceBlue">
        <TextBlock Text="{Binding personViewModel.Name}" d:Text="person.Name"/>
        <TextBlock Text="{Binding personViewModel.Age}"  d:Text="person.Age" />
    </StackPanel>-->

    <StackPanel Background="LightCoral">
        <TextBlock Text="{Binding Name}" />
        <TextBlock Text="{Binding Age}" />
        <TextBox Text="{Binding EmployeeNumber}"/>
    </StackPanel>
</UserControl>
